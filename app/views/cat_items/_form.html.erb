<%= form_with(model: cat_item, local: true) do |form| %>
  <% if cat_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cat_item.errors.count, "error") %> prohibited this cat_item from being saved:</h2>

      <ul>
      <% cat_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cat_id %>
    <%= form.text_field :cat_id %>
  </div>

  <div class="field">
    <%= form.label :cart %>
    <%= form.text_field :cart %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
